{
  "name": "cortex",
  "version": "2.1.2",
  "description": "Persistent local memory for Claude Code. Longer sessions, cross-session recall, zero cloud.",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outfile=dist/index.js --target=bun && bun build src/mcp-server.ts --outfile=dist/mcp-server.js --target=bun",
    "build:index": "bun build src/index.ts --outfile=dist/index.js --target=bun",
    "build:mcp": "bun build src/mcp-server.ts --outfile=dist/mcp-server.js --target=bun",
    "test": "bun test",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@xenova/transformers": "^2.17.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "bun-types": "^1.3.7",
    "esbuild": "^0.20.0",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "author": {
    "name": "Tomas Krajcik",
    "email": "support@rootdeveloper.dev",
    "url": "https://rootdeveloper.dev"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/hjertefolger/cortex"
  }
}
